using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BridgePuzzle : MonoBehaviour
{
    public bool isUnlocked = false;
    public bool onBridge = false;

    private void Update()
    {
        if (isUnlocked && onBridge)
        {
            
        }
        else if(isUnlocked)
        {
            
        }

    }
    
    
    
}
